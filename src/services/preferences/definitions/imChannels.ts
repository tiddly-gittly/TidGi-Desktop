import ForumIcon from '@mui/icons-material/Forum';
import type { ISectionDefinition } from './types';

export const imChannelsSection: ISectionDefinition = {
  id: 'imChannels',
  titleKey: 'Preference.IMChannels',
  Icon: ForumIcon,
  // Entirely custom — channel list, platform config, webhook mode
  items: [],
};
